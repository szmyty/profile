name: Oura Health Dashboard — Full Metrics + Unified Snapshot

on:
  push:
    branches:
      - main
      - master
  schedule:
    # Run every 6 hours (Oura data updates once daily)
    - cron: '0 */6 * * *'
  workflow_dispatch:

concurrency:
  group: profile-update
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  update-oura-health-dashboard:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          
      - name: Setup environment
        uses: ./.github/actions/setup-environment
        with:
          install-python-deps: 'false'

      - name: Fetch Oura metrics
        id: oura
        env:
          OURA_PAT: ${{ secrets.OURA_PAT }}
          OUTPUT_DIR: oura
        run: |
          mkdir -p oura logs/oura
          
          # Safe JSON write pattern: output to temp file first
          if ! scripts/fetch-oura.sh > oura/metrics.json.tmp 2>> logs/oura/oura.log; then
            echo "Error: Failed to fetch Oura data"
            rm -f oura/metrics.json.tmp
            exit 1
          fi
          
          # Validate the temp JSON file before moving
          if ! jq empty oura/metrics.json.tmp 2>/dev/null; then
            echo "Error: Invalid JSON in metrics file"
            rm -f oura/metrics.json.tmp
            exit 1
          fi
          
          # Move temp to final location (atomic operation)
          mv oura/metrics.json.tmp oura/metrics.json
          
          echo "Oura metrics fetched successfully"
          cat oura/metrics.json

      - name: Restore SVG hash cache
        uses: actions/cache@v4
        with:
          path: .cache
          key: svg-hash-cache-${{ github.run_id }}
          restore-keys: |
            svg-hash-cache-

      - name: Generate unified health snapshot
        run: |
          python scripts/generate-health-snapshot.py oura/metrics.json oura/health_snapshot.json

      - name: Generate holistic health dashboard SVG
        run: |
          python scripts/incremental-generate.py oura/health_snapshot.json oura/health_dashboard.svg scripts/generate-health-dashboard.py oura_health

      - name: Compute mood state
        run: |
          python scripts/oura-mood-engine.py oura/metrics.json oura/mood.json

      - name: Generate mood dashboard SVG card
        run: |
          python scripts/incremental-generate.py oura/mood.json oura/mood_dashboard.svg scripts/generate-oura-mood-card.py oura_mood oura/metrics.json

      - name: Optimize SVGs with SVGO
        run: |
          npm install -g svgo
          for svg in oura/health_dashboard.svg oura/mood_dashboard.svg; do
            if [ -f "$svg" ]; then
              ORIGINAL_SIZE=$(stat -c%s "$svg")
              if [ "$ORIGINAL_SIZE" -gt 0 ]; then
                svgo "$svg" -o "$svg"
                OPTIMIZED_SIZE=$(stat -c%s "$svg")
                echo "$(basename "$svg") optimized: ${ORIGINAL_SIZE} bytes → ${OPTIMIZED_SIZE} bytes ($(( 100 - OPTIMIZED_SIZE * 100 / ORIGINAL_SIZE ))% reduction)"
              else
                echo "Warning: $(basename "$svg") is empty, skipping optimization"
              fi
            else
              echo "Warning: $(basename "$svg") not found, skipping optimization"
            fi
          done

      - name: Update README with dashboard cards
        run: |
          python scripts/update-readme.py \
            --marker OURA-HEALTH-CARD \
            --content "![Oura Health Dashboard](./oura/health_dashboard.svg)"
          
          python scripts/update-readme.py \
            --marker OURA-MOOD-CARD \
            --content "![Oura Mood Dashboard](./oura/mood_dashboard.svg)"

      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # Add all relevant files
          git add oura/metrics.json oura/mood.json oura/mood_dashboard.svg oura/health_snapshot.json oura/health_dashboard.svg README.md
          
          # Add logs (always commit logs)
          git add logs/oura/
          
          # Check if there are changes to commit
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            MOOD=$(jq -r '.mood_name' oura/mood.json)
            SCORE=$(jq -r '.mood_score' oura/mood.json)
            git commit -m "🧬 Update Oura health dashboard: ${MOOD} (${SCORE}/100)"
            git push
          fi
